# Laravel Roster API – Docker Setup

##  Requirements
- Docker Desktop installed

##  How to Run

```bash
git clone https://github.com/your-name/laravel-roster-api.git
cd laravel-roster-api
cp .env.example .env
docker-compose up --build
